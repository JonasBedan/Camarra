name: camarra
description: A new Camarra project
publish_to: 'none' # This is a private project

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.30.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
